export class ConteudoModal {

  textoModal?: string;
  nomeRecebido?: string;

  constructor(textoModal: string, nomeRecebido: string  ){
    this.textoModal = textoModal;
    this.nomeRecebido = nomeRecebido;
  }
}
